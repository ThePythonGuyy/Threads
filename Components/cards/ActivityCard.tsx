import React from 'react'
import styles from '@/Styles/activity.module.scss'

interface Props {
    name: string;
    image: string;
    id: string;
}
export default function ActivityCard({ name, image, id} : Props) {
  return (
<article c>

</article>
  )
}
